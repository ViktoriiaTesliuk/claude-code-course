Write unit tests for: $ARGUMENTS

- use pytest
- create a separate test file named test_$ARGUMENTS
- place it in the same directory as the source file
- cover: happy path, edge cases, invalid input
- each test function should have a clear name that describes what it tests
